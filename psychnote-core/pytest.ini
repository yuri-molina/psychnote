[pytest]
# Garante que o diretório raiz do projeto esteja no sys.path para imports absolutos
pythonpath = .
testpaths = tests
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
markers =
    live: Testes que requerem Ollama ativo (skip automático se indisponível).
